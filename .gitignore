# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/

# Environment files — never commit secrets
.env
.env.local
.env.*.local

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.env.local

# Trained model weights
# models/*.pkl is intentionally tracked so Docker builds start with a pre-trained model.
# If the file grows too large for git, store it in Git LFS or regenerate at build time.

# Claude Code local config
.claude/

# macOS / editors
.DS_Store
.idea/
.vscode/
*.swp

# Fly.io deploy artefacts
.fly/
